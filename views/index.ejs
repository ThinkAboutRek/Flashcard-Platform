<!-- views/index.ejs -->
<!DOCTYPE html>
<html>
<head>
  <title>Flashcards</title>
</head>
<body>
  <h1>Flashcard Sets</h1>
  <ul>
    <% sets.forEach(set => { %>
      <li>
        <a href="/sets/<%= set.id %>"><%= set.name %></a>
      </li>
    <% }) %>
  </ul>
</body>
</html>
