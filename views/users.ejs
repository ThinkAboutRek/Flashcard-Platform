<!-- views/users.ejs -->
<!DOCTYPE html>
<html>
<head>
  <title>Users</title>
</head>
<body>
  <h1>Users List</h1>
  <ul>
    <% users.forEach(user => { %>
      <li><%= user.username %> (<%= user.admin ? 'Admin' : 'Regular User' %>)</li>
    <% }) %>
  </ul>
</body>
</html>
